const assert = require('assert');
const statistics = require('../index.js');

describe('statisticsFunctions', function () {
    it('has a test', function () {
        assert(false, 'statisticsFunctions should have a test');
    });
});
